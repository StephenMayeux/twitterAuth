extends layout

block content
  .container
    .row
      .col-md-12
        if !user
          h1 Sign In!!
          a(href="/auth/twitter", class="btn btn-primary intro") Sign In
        else
          h1 Hello! You are logged in as #{user.username}
          a(href="/logout", class="btn btn-primary intro") Logout
